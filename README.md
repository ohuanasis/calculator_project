# calculator_project
best approach is a layered package with one framework-independent core. The console, Flask/API, and PyQt projects become thin entry points that call the same application services.
